import React from "react";
import { useTranslations } from "next-intl";

interface Props {}

function ShopInfoSubset(props: Props) {
  const t = useTranslations("webSiteConstant");

  return <div>ShopInfoSubset</div>;
}

export default ShopInfoSubset;
